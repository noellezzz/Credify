export const selectIsLoading = (state) => state.verification.isLoading;
export const selectError = (state) => state.verification.error;
export const selectVerificationResult = (state) => state.verification.verificationResult;
export const selectStats = (state) => state.verification.stats;
export const selectStatsLoading = (state) => state.verification.statsLoading;
export const selectStatsError = (state) => state.verification.statsError;